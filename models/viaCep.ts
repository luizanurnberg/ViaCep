class viaCepData {
    localidade: string
    uf: string
    logradouro: string

    constructor(localidade, uf, logradouro) {
        localidade: this.localidade
        uf: this.uf
        logradouro: this.logradouro

    }
}

export {
    viaCepData
}